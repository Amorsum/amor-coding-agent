# Calculator demo repository

This intentionally small Python repository is the reproducible AMOR interview demo. Create an
independent Git repository from it with `scripts/create-demo-repository.ps1`; do not run AMOR
directly against this template directory.
