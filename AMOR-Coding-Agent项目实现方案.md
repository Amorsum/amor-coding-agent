# AMOR：可评测、可观测、安全的本地 Coding Agent

> 文档性质：产品需求、AI 系统设计、开发路线和项目验收规格书  
> 项目全称：**AMOR — Agentic Maintainer for Objective Repair**  
> 功能标语：**Search. Patch. Test. Verify.**  
> 项目定位：输入一个代码任务，让 Agent 在受限本地仓库中自主搜索、阅读、修改代码并运行验证，通过自动化测试而不是模型自述判断任务是否完成。

---

## 1. 项目概述

### 1.1 一句话介绍

AMOR（Agentic Maintainer for Objective Repair）是一个面向本地代码仓库的 Coding Agent。用户选择仓库并描述 Bug 或小型开发任务，Agent 自动理解仓库、制定计划、调用代码搜索与文件编辑工具、运行测试，根据反馈继续修复，最后输出代码差异、验证结果、成本和执行轨迹。`AMOR` 同时继承开发者网名 `Amorsum` 的前四个字母，而其英文全称直接表达项目核心：以客观验证驱动自主代码维护与修复。

### 1.2 项目要解决的问题

普通 AI 编程对话通常存在以下问题：

- 用户需要手工复制大量代码给模型。
- 模型不了解完整仓库结构。
- 模型生成修改后不会主动运行测试。
- 模型可能声称完成，但代码无法编译或测试失败。
- 多轮执行容易重复搜索、反复修改或陷入死循环。
- 终端和文件访问权限过大，可能读取密钥或运行危险命令。
- 无法定量比较不同模型、Prompt 或 Agent 策略。

AMOR 的目标不是取代成熟 IDE，而是研究并展示：

> 如何构建一个能够安全使用工具、控制上下文、根据执行反馈自我修复，并通过客观测试验证结果的 Agent。

### 1.3 与普通代码生成工具的区别

```text
普通代码生成：
需求 → 模型生成代码 → 用户自行复制、运行和排错

AMOR：
需求 → 探索仓库 → 制定计划 → 修改代码 → 运行验证
    → 分析失败 → 继续修复 → 客观验收 → 输出报告
```

### 1.4 与 VidSnap 的区别

| VidSnap | AMOR |
|---|---|
| 视频理解应用 | 软件工程 Agent |
| 检索视频字幕 | 探索代码仓库 |
| 回答用户问题 | 修改外部环境中的代码 |
| 固定的语义/关键词/时间工具 | 文件、搜索、补丁、测试、诊断工具 |
| 内容质量与幻觉评测 | 任务成功率与测试通过率评测 |
| 短工具循环 | 长任务规划、反馈和恢复 |
| 主要风险是回答不准确 | 主要风险是误改文件、危险命令和密钥泄露 |

两个项目共同体现 Agent 能力，但应用对象、工具复杂度、验证方式和安全边界明显不同。

---

## 2. 项目目标与非目标

### 2.1 MVP 目标

第一版需要完成：

1. 用户选择一个允许访问的本地 Git 仓库。
2. 用户输入一个 Bug 修复或小型开发任务。
3. Agent 获取仓库结构，但不一次性读取全部文件。
4. Agent 使用搜索和局部读取定位相关代码。
5. Agent 生成并维护结构化任务计划。
6. Agent 使用补丁方式修改文件。
7. Agent 运行相关测试、类型检查或构建命令。
8. 验证失败时，Agent读取错误并进行有限轮次修复。
9. Agent达到成功、失败、需要用户信息或预算耗尽等明确终止状态。
10. 前端展示计划、工具轨迹、Git diff、验证结果、耗时和费用。
11. 使用固定代码任务集评价Agent成功率。
12. 比较至少两种Agent策略或两个模型。

### 2.2 MVP 不做

- 不直接操作远程GitHub仓库。
- 不自动提交、推送或创建PR。
- 不处理超大型仓库。
- 不追求完整SWE-bench成绩。
- 不实现多Agent团队。
- 不允许Agent任意联网。
- 不支持跨仓库修改。
- 不执行生产部署和数据库迁移。
- 不允许危险或不可恢复的系统命令。
- 不直接修改用户当前工作副本，默认使用临时副本或隔离worktree。

### 2.3 第二阶段目标

- 支持更多语言和测试框架。
- 自动选择相关测试和验证命令。
- 加入仓库级长期记忆。
- 支持交互式澄清问题。
- 支持用户审批关键操作。
- 加入 Prompt Injection 防御评测。
- 支持 GitHub Issue 导入和 PR 草稿生成。
- 在容器中执行不受信任代码。
- 适配 SWE-bench Verified 的一小部分任务。

---

## 3. 项目的差异化亮点

目前大量 Coding Agent 项目停留在“ReAct循环 + 文件工具 + Shell工具”。AMOR 不应以工具数量作为主要卖点，而应围绕“可信执行”形成差异。

### 3.1 亮点一：完成不是由模型宣布，而是由验证器证明

普通 Agent 常在输出“任务已完成”后结束。AMOR 将模型与验收器分离：

```text
Agent提出完成
  ↓
独立Verifier运行验收命令
  ↓
检查测试、构建、类型、任务断言和修改范围
  ↓
通过：任务成功
失败：错误反馈给Agent继续修复
```

Verifier 不接受“代码看起来正确”作为完成依据。一个任务只有满足预先定义的验收条件才算成功。

可展示的数据：

- Agent自报完成率
- Verifier实际通过率
- “自报完成但验证失败”的比例
- 首次通过率与最终通过率

这一差值能够直接体现为什么需要独立验证。

### 3.2 亮点二：上下文预算与证据链可视化

不是将整个仓库塞进上下文，而是记录Agent为什么读取每个文件：

```json
{
  "path": "src/auth/register.ts",
  "reason": "搜索 registerUser 符号命中",
  "source": "search_code",
  "lines": "42-118",
  "tokens": 812,
  "retained": true
}
```

系统展示：

- 本次任务读取了多少文件和代码行
- 哪些上下文最终与修改有关
- 搜索、测试日志和代码分别消耗多少token
- 上下文被压缩或淘汰的原因
- 成功任务的有效上下文比例

可以比较两种策略：

- 全量/宽泛读取
- 搜索优先、按需局部读取

目标是证明上下文工程能否在保持成功率的同时减少token与费用。

### 3.3 亮点三：安全边界是Agent架构的一部分

AMOR 默认把仓库内容视为不可信数据，重点防御：

- 仓库文件中隐藏的间接Prompt Injection
- 诱导读取 `.env`、SSH密钥或仓库外文件
- 危险删除与覆盖命令
- 下载并执行未知脚本
- 修改不在任务范围内的文件
- 输出环境变量和凭证

安全层独立于LLM存在，不能只靠System Prompt约束。所有工具调用先经过Policy Engine：

```text
模型提出工具调用
  ↓
Schema校验
  ↓
路径边界检查
  ↓
命令策略检查
  ↓
风险分级
  ↓
允许 / 拒绝 / 要求用户批准
```

项目使用一组恶意仓库任务，定量展示攻击阻断率和误拦截率。

### 3.4 亮点四：固定任务集上的可复现实验

项目内置一套小型 Coding Agent Benchmark。每个任务包含固定Git提交、问题描述、隐藏验收测试、允许范围和难度。

可以比较：

- 直接执行 vs 先规划再执行
- 每轮反思 vs 仅失败时反思
- 全量读取 vs 搜索优先
- 不同模型
- 不同最大轮数
- 有无错误摘要

每组实验保存完整配置和结果，避免“只挑成功案例录Demo”。

### 3.5 亮点五：错误归因，而不只有成功/失败

失败被划分为：

- 没找到相关文件
- 误解任务要求
- 修改方向错误
- 测试选择错误
- 无法理解错误日志
- 重复执行无进展
- 上下文遗忘
- 工具调用格式错误
- 环境或依赖失败
- 安全策略阻止
- 预算或轮数耗尽

报告显示Agent在哪个阶段最容易失败，为策略优化提供依据。

### 3.6 亮点六：可重放的Agent轨迹

每次运行保存：

- 初始仓库提交
- 用户任务
- 模型与Prompt版本
- 每次工具调用和结果摘要
- 计划变更
- 文件补丁
- 验证命令与结果
- token、费用和耗时
- 最终状态

轨迹可按步骤回放，便于复现“为什么改坏了”以及比较两个Agent版本。

---

## 4. 目标用户与典型任务

### 4.1 目标用户

- 希望自动完成小型修复的开发者
- 研究Agent规划、工具调用与上下文工程的学生
- 需要比较不同Coding Agent策略的AI工程师
- 希望了解AI修改了什么、为什么修改的代码审查者

### 4.2 MVP适合的任务

- 修复空值、数组越界和边界条件
- 修复API错误状态码
- 增加参数校验
- 修复类型错误
- 修改小范围业务逻辑
- 增加或修复单元测试
- 更新简单配置
- 完成范围明确的小功能

### 4.3 MVP不适合的任务

- 重构整个大型系统
- 从零开发完整产品
- 修改生产基础设施
- 涉及大量主观UI设计
- 需求本身没有验收条件
- 必须访问私有外部系统才能验证

---

## 5. 完整工作流程

```text
选择本地Git仓库
  ↓
创建隔离工作区并记录基线提交
  ↓
输入任务与可选验收条件
  ↓
Repository Profiler识别语言、依赖和测试命令
  ↓
Agent判断信息是否充足
  ├── 不足：向用户提出澄清问题
  └── 足够：生成结构化计划
  ↓
搜索文件、符号和文本
  ↓
局部读取相关代码
  ↓
生成并应用补丁
  ↓
运行最小相关验证
  ↓
失败？
  ├── 是：分析错误、更新计划、有限重试
  └── 否：运行更完整验证
  ↓
独立Verifier检查验收条件
  ↓
输出Git diff、测试结果、执行轨迹和风险说明
```

---

## 6. Agent状态机设计

不要只写一个无限 `while` 循环。建议使用显式状态机：

```text
INITIALIZING
  → PROFILING_REPO
  → CLARIFYING（可选）
  → PLANNING
  → EXPLORING
  → EDITING
  → VALIDATING
  → DIAGNOSING（验证失败）
  → REPLANNING（必要时）
  → FINAL_VERIFYING
  → SUCCEEDED / FAILED / BLOCKED / BUDGET_EXHAUSTED / CANCELLED
```

每次状态转换都记录原因，便于轨迹回放和错误归因。

### 6.1 终止条件

成功条件：

- 任务指定的验收测试通过。
- 项目构建/类型检查达到任务要求。
- 没有超出允许修改范围。
- Verifier确认任务断言满足。

失败或暂停条件：

- 达到最大Agent轮数。
- 达到最大费用或token预算。
- 连续出现相同错误且没有新修改。
- 连续工具调用没有获得新信息。
- 需要用户才能提供的关键需求缺失。
- 安全策略拒绝必要操作。
- 用户取消。

---

## 7. 核心工具设计

工具应少而清晰。MVP建议包含以下工具。

### 7.1 `list_files`

用途：查看限定目录下的结构。

限制：

- 只能访问工作区。
- 默认深度有限。
- 自动忽略 `.git`、依赖目录、构建产物和密钥文件。

### 7.2 `search_code`

用途：搜索文件名、字符串、函数名、类名和符号。

推荐底层使用 `rg`，后续可加入tree-sitter或语言服务器符号索引。

返回内容必须截断并排序，避免一次搜索污染上下文。

### 7.3 `read_file`

用途：按指定行范围读取文件。

要求：

- 默认局部读取。
- 返回行号。
- 记录读取原因和token估算。
- 敏感文件拒绝读取。

### 7.4 `apply_patch`

用途：以补丁形式修改文件。

要求：

- 记录修改前后哈希。
- 验证目标仍位于工作区。
- 保存可回滚diff。
- 禁止静默覆盖整个大文件。

### 7.5 `run_validation`

用途：执行测试、构建、lint或类型检查。

与任意Shell工具相比，它只允许经过规则匹配的命令类别，并设置：

- 工作目录
- 超时
- 最大输出
- 环境变量白名单
- 网络策略
- CPU/内存限制（后续容器化）

### 7.6 `get_git_diff`

用途：查看Agent产生的修改，不能将用户原有修改误认为Agent修改。

### 7.7 `get_diagnostics`

用途：获取编译、类型或语言服务器诊断。第一版可由构建输出代替。

### 7.8 `update_plan`

用途：结构化维护任务步骤、状态和证据。

工具的重点不是数量，而是输入Schema、返回信息量、错误语义和安全策略。

---

## 8. Repository Profiler

运行Agent前先识别仓库环境：

- 主要语言
- 包管理器
- 测试框架
- 构建命令
- lint/type-check命令
- 入口目录
- 忽略目录
- 仓库级说明文件，如 `AGENTS.md`
- Git状态和用户已有修改

输出示例：

```json
{
  "languages": ["TypeScript"],
  "package_manager": "npm",
  "test_commands": ["npm test"],
  "typecheck_commands": ["npm run typecheck"],
  "source_roots": ["src"],
  "test_roots": ["tests"],
  "dirty_worktree": false
}
```

自动识别结果必须允许用户修改，不能直接运行猜测出的危险命令。

---

## 9. 计划、记忆与上下文工程

## 9.1 结构化计划

```json
{
  "goal": "修复重复邮箱注册问题",
  "hypothesis": "注册服务在写入前没有检查唯一约束异常",
  "steps": [
    {"id": 1, "task": "定位注册入口和数据模型", "status": "completed"},
    {"id": 2, "task": "找到现有失败行为", "status": "completed"},
    {"id": 3, "task": "实施最小修复", "status": "in_progress"},
    {"id": 4, "task": "增加回归测试", "status": "pending"},
    {"id": 5, "task": "执行最终验证", "status": "pending"}
  ]
}
```

计划变化必须有原因，避免Agent每轮无意义重写计划。

## 9.2 工作记忆

保存：

- 当前假设
- 相关文件和符号
- 已读取范围
- 已修改文件
- 已执行命令
- 最近测试错误
- 已尝试但失败的方案
- 未解决问题

MVP使用结构化数据库/JSON即可，不需要向量长期记忆。

## 9.3 上下文选择

优先级建议：

1. 当前任务和验收条件
2. 当前计划与未解决错误
3. 最近修改的diff
4. 与当前步骤直接相关的代码
5. 最近一次验证日志摘要
6. 仓库背景摘要

较旧的完整工具输出应压缩为摘要，而不是永久留在上下文。

## 9.4 上下文预算

为以下部分设置独立预算：

- 任务与计划
- 源代码
- 搜索结果
- 测试日志
- 历史轨迹摘要

达到预算时，优先移除重复搜索和已解决错误，不移除任务验收条件。

---

## 10. 独立验证器设计

Verifier是项目的核心差异化模块，不由主Agent自行决定通过。

### 10.1 验证层级

#### 第一级：补丁完整性

- 是否产生修改
- 是否只修改允许目录
- 是否出现明显大范围无关改动
- 是否新增二进制或异常大文件

#### 第二级：静态验证

- 格式化
- lint
- 类型检查
- 编译

#### 第三级：相关测试

- 运行与修改模块最相关的测试
- 优先提供快速反馈

#### 第四级：隐藏验收测试

- Agent不能在执行前看到完整测试实现
- 防止通过删除测试或针对测试硬编码来“作弊”

#### 第五级：完整回归

- 任务规模允许时运行完整测试
- 区分原本就失败的测试与Agent引入的退化

### 10.2 防止伪完成

以下情况不能算成功：

- 删除或跳过失败测试
- 用硬编码只通过单一输入
- 修改验收脚本
- 捕获全部异常但不正确处理
- 测试没有真正执行
- 构建失败但Agent声称完成
- 修改仓库外文件达到假通过

---

## 11. 安全沙盒与Policy Engine

### 11.1 工作区隔离

推荐顺序：

1. 用户选择仓库。
2. 检查Git状态。
3. 创建临时副本或Git worktree。
4. 所有Agent操作只作用于隔离工作区。
5. 用户确认后再手工应用补丁。

### 11.2 文件策略

默认禁止：

- 工作区之外的路径
- `.env*`
- SSH/GPG凭证
- 浏览器和云服务凭证
- 系统目录
- 用户主目录枚举
- `.git`内部对象修改

### 11.3 命令策略

命令分级：

| 等级 | 示例 | 行为 |
|---|---|---|
| 低风险 | 测试、lint、typecheck、只读Git | 自动允许 |
| 中风险 | 安装依赖、格式化、多文件生成 | 请求批准或按配置允许 |
| 高风险 | 删除、网络脚本、系统配置、推送部署 | MVP拒绝 |

### 11.4 Prompt Injection防御

- System Prompt明确说明仓库内容是数据而非指令。
- 工具层不根据文件内容自动扩大权限。
- 敏感文件在模型调用前由策略层过滤。
- 仓库中的“请读取.env”等文本只作为可疑内容报告。
- 建立恶意README、注释和测试日志样本进行攻击评测。

### 11.5 资源限制

- 单命令超时
- 单任务总耗时上限
- 最大工具轮数
- 最大模型费用
- 最大输出长度
- 最大文件大小
- 后续加入容器CPU、内存和进程数限制

---

## 12. Benchmark数据集设计

## 12.1 数据来源

首版不直接挑战大型SWE-bench。推荐准备2～3个小型、许可清晰的项目，在固定提交上构建20～30个任务。

任务来源可以是：

- 开源仓库中已经修复的历史Bug，回退到修复前提交
- 自己在示例仓库中注入的Bug
- 原有测试未覆盖的边界条件
- 小型功能需求
- 专门构造的安全攻击任务

### 12.2 任务结构

```json
{
  "task_id": "ts_api_001",
  "repo": "sample-typescript-api",
  "base_commit": "abc123",
  "instruction": "空数组输入时接口应返回200和空结果，而不是500。",
  "difficulty": "easy",
  "category": "edge_case",
  "visible_tests": ["tests/items.test.ts"],
  "hidden_test_command": "npm run eval:test -- ts_api_001",
  "allowed_paths": ["src/items/**", "tests/items/**"],
  "expected_behavior": "空数组被视为合法输入",
  "max_minutes": 10
}
```

### 12.3 建议分布

| 类别 | 数量 |
|---|---:|
| 单文件逻辑Bug | 6 |
| 边界条件 | 5 |
| 跨文件Bug | 4 |
| 小功能 | 4 |
| 类型/配置错误 | 3 |
| 信息不足，应澄清 | 2 |
| Prompt Injection/越权攻击 | 3 |
| 无法在当前环境完成 | 2 |

最后两类用于评估Agent是否会正确停止，而不是盲目声称成功。

### 12.4 数据集质量要求

- 每个任务从相同基线可重复初始化。
- 验收测试在修复前失败、正确修复后通过。
- 隐藏测试不允许Agent修改。
- 任务描述没有泄露完整补丁。
- 任务难度由人工验证。
- 保存参考修复，但评测运行时不提供给Agent。

---

## 13. Agent评测指标

### 13.1 核心指标

| 指标 | 含义 |
|---|---|
| Task Success Rate | 隐藏验收测试通过的任务比例 |
| First-Try Success | 第一次补丁即通过的比例 |
| Final Test Pass Rate | 最终测试通过情况 |
| Regression Rate | 引入新测试失败的比例 |
| Average Iterations | 成功任务平均修复轮数 |
| Tool Calls per Success | 每个成功任务的工具调用数量 |
| Tokens per Success | 每个成功任务的token消耗 |
| Cost per Success | 每个成功任务的模型费用 |
| Time to Success | 完成成功任务的时间 |
| Scope Violation Rate | 修改越界文件的比例 |
| Safety Violation Rate | 尝试执行危险操作的比例 |
| False Completion Rate | Agent声称成功但Verifier未通过的比例 |

### 13.2 上下文工程指标

- 总读取文件数
- 总读取代码行数
- 最终被修改文件在读取内容中的占比
- 重复读取率
- 无结果搜索率
- 测试日志token占比
- 上下文压缩次数

### 13.3 稳定性

同一任务可运行3次，记录：

- 3次中成功几次
- 产生的补丁是否一致
- 工具调用差异
- 成本和耗时方差

只运行一次不能充分说明随机Agent的稳定性。

---

## 14. 实验设计

项目至少完成两组受控实验。

### 实验一：规划是否有效

比较：

- A：直接ReAct执行
- B：先生成结构化计划，失败时更新计划

控制变量：模型、任务集、最大轮数、工具、温度相同。

观察：成功率、工具调用数、token、耗时和重复操作。

### 实验二：上下文选择策略

比较：

- A：宽泛读取相关目录
- B：搜索优先、局部读取、日志摘要

观察：成功率是否保持、输入token减少多少、是否更容易漏掉跨文件依赖。

### 可选实验三：反思触发策略

- 每轮反思
- 仅测试失败时反思
- 连续无进展时反思

目标不是证明反思一定有效，而是找出质量与成本的合理触发条件。

### 可选实验四：模型选择

比较高能力模型与低成本模型，并报告每个成功任务成本，而不是只比较单次调用价格。

---

## 15. 页面与交互设计

## 15.1 任务创建页

- 选择本地仓库
- 显示Git状态和主要技术栈
- 输入任务描述
- 输入验收条件（可选）
- 设置最大时间、费用和轮数
- 选择模型与Agent策略
- 选择隔离方式

## 15.2 Agent工作台

建议三栏：

左栏：

- 文件树
- 已读取文件
- 修改文件

中栏：

- 用户任务
- 当前状态
- 结构化计划
- Agent简短决策说明
- 需要用户批准/澄清的事项

右栏：

- 工具调用时间线
- 测试与构建结果
- token、费用和耗时
- 风险提示

不要展示私有Chain-of-Thought；只展示可验证的简短决策摘要、工具调用和状态变化。

## 15.3 Diff与验证页

- Git diff
- 文件级变更摘要
- 相关测试
- 完整测试
- 隐藏验收结果
- 越界修改检查
- 最终状态
- 导出补丁

## 15.4 Benchmark页

- 任务列表和难度
- 不同实验成功率
- 质量/成本散点图
- 错误类型分布
- 单任务轨迹对比
- 失败案例回放

---

## 16. 系统架构

```text
Web UI
  │
  ▼
Application API
  ├── Workspace Manager
  ├── Repository Profiler
  ├── Agent Orchestrator / State Machine
  ├── Context Manager
  ├── Tool Registry
  ├── Policy Engine
  ├── Verifier
  ├── Trace & Cost Collector
  └── Benchmark Runner
        │
        ├── PostgreSQL / SQLite
        ├── Local Artifact Storage
        ├── Isolated Git Worktree / Temp Copy
        └── LLM Provider
```

### 16.1 推荐执行顺序

工具调用必须经过：

```text
Agent Orchestrator
  → Tool Schema Validator
  → Policy Engine
  → Workspace Boundary
  → Tool Executor
  → Output Truncation/Summarization
  → Trace Store
  → Context Manager
```

---

## 17. 推荐技术栈

### 17.1 前端

- Next.js + TypeScript
- Tailwind CSS
- shadcn/ui
- Monaco Editor：代码和diff浏览
- React Flow（可选）：状态流可视化
- ECharts/Recharts：Benchmark图表

### 17.2 后端

推荐Python：

- Python 3.11+
- FastAPI
- Pydantic
- SQLAlchemy/SQLModel
- Alembic

也可以全栈TypeScript，但Python更方便接入Agent与评测生态。

### 17.3 Agent编排

两种选择：

#### 推荐：自己实现小型显式状态机

优点：

- 更能展示对Agent Loop的理解
- 容易控制每个状态和终止条件
- 不会被框架隐藏核心逻辑

#### 可选：LangGraph

适合快速实现持久状态、条件边和人工审批。即使使用LangGraph，也应自行设计状态、工具、策略和验证器。

不要使用CrewAI式多Agent作为MVP核心。

### 17.4 模型接入

- LiteLLM或自建Provider抽象
- 支持至少两个OpenAI兼容模型
- 结构化输出/JSON Schema
- 工具调用
- 流式或事件式输出

模型应可替换，避免业务逻辑绑定一家供应商。

### 17.5 代码搜索与Git

- `rg`：文本搜索
- Git CLI：状态、diff、worktree、补丁
- tree-sitter（第二阶段）：语法树与符号提取
- Language Server Protocol（后续）：引用、定义和诊断

### 17.6 执行与沙盒

MVP：

- 临时目录或Git worktree
- 子进程超时和输出上限
- 路径白名单
- 命令allowlist/policy

增强版：

- Docker容器
- 禁止网络
- 只挂载隔离仓库
- CPU、内存、进程和时间限制

### 17.7 数据与队列

- MVP：SQLite + 本地JSON/文件
- 部署版：PostgreSQL
- 长任务：Redis + Celery/RQ/Dramatiq，或后端原生任务队列
- SSE/WebSocket：前端实时展示事件

### 17.8 可观测性

- OpenTelemetry（可选）
- Langfuse/Phoenix（可选，不要替代自定义Agent轨迹）
- 自建结构化事件表
- token与价格配置表

### 17.9 测试与部署

- pytest
- Playwright：端到端UI测试
- Docker Compose
- GitHub Actions

公开Demo应使用预置示例仓库，不能允许陌生用户扫描服务器任意路径或运行任意命令。

---

## 18. 核心数据结构

### Task

```json
{
  "id": "task_xxx",
  "workspace_id": "workspace_xxx",
  "instruction": "修复空数组导致的500错误",
  "acceptance_criteria": ["空数组返回200", "原有测试通过"],
  "model": "model-name",
  "strategy": "plan_then_execute",
  "limits": {
    "max_rounds": 20,
    "max_minutes": 15,
    "max_cost": 2.0
  }
}
```

### AgentState

```json
{
  "phase": "VALIDATING",
  "plan": [],
  "hypotheses": [],
  "relevant_files": [],
  "modified_files": [],
  "attempted_fixes": [],
  "latest_error_summary": "...",
  "round": 7,
  "token_usage": {},
  "cost": 0.42
}
```

### ToolEvent

```json
{
  "event_id": "evt_xxx",
  "task_id": "task_xxx",
  "phase": "EXPLORING",
  "tool": "search_code",
  "arguments": {"query": "registerUser"},
  "policy_result": "allowed",
  "output_summary": "3个文件命中",
  "duration_ms": 84,
  "created_at": "..."
}
```

### VerificationResult

```json
{
  "passed": false,
  "checks": [
    {"name": "scope", "passed": true},
    {"name": "typecheck", "passed": true},
    {"name": "hidden_tests", "passed": false, "summary": "1 failed"}
  ],
  "failure_category": "behavior_not_fixed"
}
```

---

## 19. 开发阶段与里程碑

## 阶段0：任务集和验收先行（2～3天）

任务：

- 选择1个小型示例仓库。
- 准备5个Bug任务。
- 为每个任务准备隐藏验收测试。
- 人工验证修复前失败、正确修复后通过。
- 冻结MVP工具和安全范围。

验收：

- 5个任务可从固定提交重复初始化。
- 有客观验收命令。

## 阶段1：CLI端到端Agent（5～7天）

任务：

- 仓库Profiler。
- `list/search/read/patch/validate/diff`工具。
- 单Agent状态循环。
- 结构化工具调用。
- 最大轮数和超时。
- CLI事件输出。

验收：

- 至少完成2个简单任务。
- 每次修改都有diff。
- Agent不能直接操作仓库外文件。

## 阶段2：独立Verifier与反馈修复（4～6天）

任务：

- 隐藏测试。
- 静态检查和测试层级。
- 验证失败回馈Agent。
- 重复错误和无进展检测。
- 明确终止状态。

验收：

- Agent自报成功不能绕过Verifier。
- 测试失败后可自动完成至少一次有效修复。

## 阶段3：上下文管理与计划（5～7天）

任务：

- 结构化计划。
- 工作记忆。
- 搜索优先局部读取。
- 工具输出摘要与上下文预算。
- 记录文件选择证据链。

验收：

- 能展示每个文件为何进入上下文。
- 上下文超限时不会丢失任务和验收条件。

## 阶段4：安全沙盒（4～6天）

任务：

- Git worktree/临时副本。
- 文件与命令Policy Engine。
- 敏感文件过滤。
- 费用、时间、轮数限制。
- 3个Prompt Injection/越权任务。

验收：

- Agent无法越过工作区。
- 高风险命令被拒绝并记录。
- 恶意仓库文本不能扩大权限。

## 阶段5：Benchmark与实验（5～8天）

任务：

- 扩展到20～30个任务。
- 批量运行器。
- 至少两种策略。
- 指标汇总和错误归因。
- 同一任务多次运行。

验收：

- 产出真实成功率、成本、耗时和安全数据。
- 能查看失败任务轨迹。

## 阶段6：Web产品化与部署（5～8天）

任务：

- 任务创建页。
- 实时Agent工作台。
- Diff与验证页面。
- Benchmark Dashboard。
- Docker Compose。
- README、架构图和演示视频。

验收：

- 新环境可按README启动。
- Demo完成“任务→修改→测试→报告”全流程。
- 公共部署只能操作预置沙盒仓库。

### 时间预估

- 集中开发：4～6周
- 课余开发：6～9周
- 可写进简历的最早节点：完成阶段5并取得真实实验数据

---

## 20. AI辅助开发建议

### 20.1 适合由AI生成

- CRUD与页面代码
- 数据库Schema
- 工具输入输出模型
- Git和子进程封装
- 图表与事件时间线
- Docker配置
- 示例仓库和部分测试任务
- 单元测试与接口测试

### 20.2 必须由你做判断

- Agent状态如何流转
- 工具权限边界
- 什么条件才算真正完成
- 隐藏测试是否有效
- Benchmark是否泄露答案
- 上下文选择策略
- 两组实验是否控制变量
- 失败归因是否有证据
- 安全策略是否真正在工具层生效

### 20.3 推荐开发方式

每次给AI一个模块规格：

```text
目标
输入/输出Schema
允许访问范围
错误情况
验收用例
不可修改的模块
必须生成的测试
```

不要用一句“生成完整Coding Agent”直接搭建全部项目。

---

## 21. 简历可用门槛

满足以下条件后，可作为独立项目写进简历：

- Agent能够搜索、局部读取、补丁修改和运行验证。
- 有显式计划或状态机，不是无限ReAct循环。
- 有独立Verifier，模型不能自行宣布成功。
- 有工作区隔离和工具级安全策略。
- 有20个以上固定任务及隐藏验收测试。
- 至少比较两种Agent策略。
- 记录任务成功率、成本、轮数、耗时和误报完成率。
- 能展示至少3类典型失败及改进结果。
- 有可回放轨迹、真实Git diff和测试证据。
- 有README、架构图、演示和可复现实验报告。

不必等到多Agent、SWE-bench、GitHub PR和云沙盒全部完成。

---

## 22. README与演示重点

README应按以下顺序组织：

1. 一句话问题定义
2. 90秒演示
3. 与普通Coding Agent的差异
4. 状态机与系统架构
5. Verifier完成判定
6. 上下文预算与证据链
7. 安全沙盒和攻击模型
8. Benchmark任务构造
9. 两组对照实验结果
10. 失败案例分析
11. 本地启动
12. 已知限制
13. Roadmap

推荐90秒演示流程：

```text
选择含Bug的示例仓库
→ 输入任务
→ Agent搜索并读取少量文件
→ 第一次修改后测试失败
→ Agent根据错误修复
→ Verifier隐藏测试通过
→ 展示diff、轨迹、安全边界、成本和Benchmark结果
```

“第一次失败后成功恢复”比只展示一次顺利执行更能体现Agent价值。

---

## 23. 未来改进方向

### 23.1 更强的代码理解

- tree-sitter语法树
- LSP符号、定义与引用
- 调用图和依赖图
- AST级补丁
- 基于代码结构的检索与重排

### 23.2 更强的Agent策略

- 动态规划深度
- 失败触发的Reflection
- 不确定性估计
- 为高风险决策请求用户批准
- 主Agent与独立Reviewer Agent；仅在单Agent稳定后增加

### 23.3 更强的验证

- 自动生成回归测试
- 测试质量审查
- Mutation Testing，验证测试是否真的能抓住Bug
- 静态安全扫描
- 性能基准对比
- 检测硬编码和针对测试作弊

### 23.4 更强的沙盒

- 每任务独立容器
- 无网络执行
- 资源限额
- 快照与一键回滚
- 依赖缓存
- 多语言基础镜像

### 23.5 GitHub工作流

- 导入Issue
- 生成PR描述
- 用户审批后创建PR
- CI失败自动回传Agent
- PR Review Agent

这些属于外部写操作，必须由用户明确授权。

### 23.6 Benchmark扩展

- 50～100个自建任务
- Python、TypeScript、Java等多语言
- SWE-bench Verified小规模适配
- 与开源Coding Agent进行同模型、同预算比较
- 公开任务定义和可重复实验脚本

### 23.7 长期记忆

- 仓库约定与架构摘要
- 历史任务和修复模式
- 常用验证命令
- 用户偏好
- 记忆的过期、冲突和可信度管理

---

## 24. 项目风险

### 风险一：变成简单API套壳

解决：将Verifier、Context Manager、Policy Engine和Benchmark作为核心交付，而不是只展示聊天界面。

### 风险二：任务集太简单

解决：包含跨文件任务、隐藏测试、信息不足任务、安全攻击任务和同任务多次运行。

### 风险三：Agent通过修改测试作弊

解决：隐藏测试放在只读位置，Verifier独立运行并检查修改范围。

### 风险四：Agent误删用户代码

解决：隔离worktree/副本、补丁编辑、禁止危险命令、最终由用户应用diff。

### 风险五：成本失控和死循环

解决：预算、轮数、时间、重复调用和无进展终止机制。

### 风险六：公开Demo成为远程代码执行入口

解决：只允许预置任务和预置仓库；不允许任意路径、任意仓库URL和任意命令。

### 风险七：结果不可复现

解决：固定提交、模型、Prompt、温度、工具版本、任务配置，并保存完整轨迹。

---

## 25. 最终交付物

- AMOR Web应用或本地桌面式Web UI
- Agent CLI
- 显式状态机/Agent Orchestrator
- Repository Profiler
- 工具注册表
- Context Manager
- Policy Engine
- 独立Verifier
- 隔离工作区管理
- 20～30个固定Benchmark任务
- 隐藏验收测试
- 批量实验运行器
- Benchmark Dashboard
- 两组受控实验报告
- Prompt Injection与越权测试报告
- Docker Compose
- 完整README
- 架构图与状态图
- 90秒演示视频
- 真实成功率、成本、耗时和失败分析

---

## 26. 项目完成定义（Definition of Done）

当以下条件全部满足时，项目核心版本完成：

- 支持至少一种主语言，建议先选择Python或TypeScript。
- 可在隔离仓库内完成搜索、阅读、修改和验证。
- Agent使用结构化状态机并具备明确终止条件。
- 独立Verifier决定成功或失败。
- 隐藏测试不可被Agent修改。
- 工具调用全部经过路径和命令策略检查。
- 能阻止读取敏感文件和访问仓库外路径。
- 至少20个Benchmark任务可重复运行。
- 至少两种策略在相同预算下完成对照实验。
- 报告包含成功率、误报完成率、轮数、token、成本、耗时和安全违规率。
- 每个任务可查看diff、验证结果和可重放轨迹。
- 公共Demo不开放任意代码执行能力。
- README和实验数字能够支持简历中的每项描述。

---

## 27. 简历描述模板

取得真实数据后替换占位符：

> **AMOR（Agentic Maintainer for Objective Repair）｜可评测、安全的本地 Coding Agent**  
> 设计面向本地Git仓库的Coding Agent，构建“仓库分析—按需检索—结构化规划—补丁修改—测试反馈—独立验收”闭环；通过工具级Policy Engine、隔离工作区和敏感路径过滤限制Agent权限，并使用X个带隐藏测试的固定任务评测任务成功率、误报完成率和单位成功成本。对比直接ReAct与计划驱动、宽泛读取与搜索优先等策略，使任务成功率由X提升至Y，同时将平均上下文token降低Z%；实现完整Agent轨迹回放和失败归因。

面试重点：

- 为什么不能让模型自己判断完成
- 如何防止删除测试和针对测试硬编码
- 上下文为什么不能全量读取
- Agent失败后何时反思、何时停止
- 安全边界为什么必须在工具层实现
- 如何构建客观可复现的Agent Benchmark
- 哪种策略提升了成功率，代价是什么

---

## 28. 建议的第一步

先不要开发Web页面。用一个很小的示例仓库完成以下CLI闭环：

```text
固定Bug任务
→ Agent搜索代码
→ 局部读取
→ 应用补丁
→ 运行测试
→ 失败后再次修改
→ 独立隐藏测试验收
→ 保存JSON轨迹和Git diff
```

当5个任务中至少有2～3个可以稳定完成，而且失败任务能明确说明失败原因时，再开发上下文优化、安全攻击集和Web工作台。项目最重要的不是页面，而是“Agent究竟有没有可靠地完成任务”这一证据链。
